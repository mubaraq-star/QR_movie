<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Scan QR Code</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 1rem; text-align: center; }
    img { max-width: 300px; height: auto; margin: 1rem 0; }
    p { font-size: 0.9rem; color: #555; }
  </style>
</head>
<body>
  <h1>Scan this QR Code</h1>
  <img src="<%= qrImage %>" alt="QR Code" />
  <p>Auto-refresh in <%= refreshSec %>s</p>
  <script>
    setTimeout(() => location.reload(), <%= refreshSec * 1000 %>);
  </script>
</body>
</html>